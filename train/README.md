# Train Directory

This directory contains Python training entrypoints.

- `finetune_muon.py`: main CLM/SFT trainer used by the shell launchers in `runs/`.

Typical usage comes indirectly through versioned launch scripts such as:

```bash
bash runs/run_v16.sh
```
